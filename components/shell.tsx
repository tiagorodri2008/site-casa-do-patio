import {Locale,restaurant,route} from '@/lib/restaurant';
import {copy} from '@/lib/copy';
import {Header,MobileActions} from './navigation';
import {ArrowUpRight,MapPin} from 'lucide-react';
export function Shell({locale,slug='',children}:{locale:Locale;slug?:string;children:React.ReactNode}){const pt=locale==='pt';return <div lang={locale==='pt'?'pt-PT':'en'}><Header locale={locale} slug={slug}/>{children}<footer className="footer"><div className="footer-top"><a className="brand" href={route(locale)}>casa do pátio<span>{copy.tagline[locale]}</span></a><a className="footer-location" href={restaurant.mapUrl} target="_blank" rel="noopener noreferrer"><MapPin size={16}/><span><small>{pt?'Localização provisória':'Temporary location'}</small>{restaurant.location[locale]}<br/>{copy.hoursShort[locale]}</span></a><a className="text-link" href={route(locale,'reservas')}>{copy.book[locale]}<ArrowUpRight size={16}/></a></div><div className="footer-bottom"><div><strong>{copy.demo[locale]}</strong><p>{copy.demoFooter[locale]}</p></div><div className="legal-links"><a href={route(locale,'privacidade')}>{copy.privacy[locale]}</a><a href={route(locale,'cookies')}>{copy.cookies[locale]}</a><a href={route(locale,'termos')}>{copy.terms[locale]}</a></div></div></footer><MobileActions locale={locale} slug={slug}/></div>}
